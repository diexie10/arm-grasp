# 诚实声明与技术债务清单

> 记录所有"推测值""暂未验证的假设""无法在 CI 中测试的路径"。核心纪律：**不要假装**。推测值不可怕，假装是验证过的才可怕。

| 条目 | 类型 | 位置 | 说明 | 计划处理时间 |
|------|------|------|------|------------|
| IK 连杆参数 L1=72/L2=105/L3=128mm | 官方参数（待实测复核） | config.py | 来源：官方示例 6DOF_RobotArm.ino（YFROBOT 热销6自由度异形机械手臂，用户确认购买型号，2026-08-18）；已回填 config.py，IK 闭环验证通过（FK(IK) 误差 <1mm）；**拼装后仍需直尺实测复核**（L4=170 为 J4 到夹爪尖，本项目 IK 只用到 J4 不配置） | 到货后实测复核 |
| 红外极性（遮挡=0） | 未验证假设 | 固件 main.c | QT30CM 模块极性未实测；若相反需固件取反一行 | 到货后 `I` 命令实测 |
| QT30CM 输出类型（推挽/开漏） | 未验证假设 | 硬件接线 | 推挽则内部上拉够用；开漏需外部 4.7kΩ 上拉 | 到货后万用表实测 |
| 关节限位值 J1[0,180] 等 | 推测值 | 固件+config.py | 舵机角域限位（固件 [0,30,10,0,45,30]/[180,180,150,180,135,120]），未实测机械限位 | 到货后实测调整 |
| JOINT_OFFSET 安装假设 | 推测值 | config.py | **关键**：J2 offset 90（舵机90°=水平）、J3 offset 0（舵机角=关节角）、J4/J5 offset 90。机械安装不同则 IK 全错 | 到货后逐关节实测"舵机 90° 时机械位置" |
| 过冲量 15° | 推测值 | 上位机 trajectory.py（待建） | 手册建议值（>最大虚位 8°），未实测 | Phase E 联调验证 |
| S 曲线参数 30°/s, 15°/s² | 推测值 | 上位机 config.py（待建） | 手册建议值（MG996 极限 60°/s 的 50%），未实测 | Phase E 调参 |
| 舵机死区 8~15μs | 推测值 | 知识库 | 手册值，未实测 | 抖动排查时验证 |
| USB 热插拔行为 | 不可验证 | 固件 | 无法在编译期验证，需真机 | Phase E 联调 |
| **USB 断开检测不可靠** | 设计事实 | 固件 usbd_conf.c | **STM32F103 USB IP 无物理断开中断**，`HAL_PCD_DisconnectCallback` 永不触发（已加注释）；拔线实际走 3ms 无 SOF→SUSP 挂起，挂起回调不停 PWM。**PWM 停止唯一兜底 = PC 端 10s 命令超时**（`CDC_TimeoutCheck`）。拔线后臂保持最后姿态最多 10s 才 limp | 已记录，Phase E 实测确认 |
| 堆余量仅 ~480B | 设计事实 | 固件 usbd_conf.c | `USBD_CDC_Init` malloc ~544B，堆 1KB（startup 0x400），余量 ~480B。够用但紧；若未来加 USB 功能需扩堆 | 已记录 |
| ~~**boot 时 6 舵机同时上电**~~ | ✅ 已修复（2026-08-21） | 固件 usbd_cdc_interface.c | **已修复**：boot 不再启动任何 PWM；每通道由首条 M 命令经 Servo_SetAngle 懒启动（servo_enabled 标志 + estop 门控），PC 软启动（M1..M6 逐个 200ms）真正生效；timeout 恢复只清标志不批量使能；H 命令保留全恢复（保持电流场景，注释说明）。同时修复 boot 脉宽矛盾（Pulse=1500 硬编码 → SERVO_HOME_PULSE=1166 宏推导，消除上电先跳 135° 再回 90° 的猛转） | 已完成 ✅ |
| **USB CDC 双轨残留** | ✅ 已清理（2026-08-21） | 固件 usbd_cdc_interface.c | **已清理**：CDC_Receive_FS 不再喂 rx_ring（USART3 唯一命令入口）；CDC_Init_FS 删隐蔽使能路径；CDC_DeInit_FS 改纯 stub（USB 总线事件不再停舵机/清 ring——主机重启不能让抓着的物体掉落）；删除死变量 usb_disconnected/tx_busy 及 M 命令路径的 usb_disconnected 检查（USB 弃用后该检查会卡 1 拒绝所有 USART3 命令）；孤儿头 stm32f103xb_new.h/stm32f1xx_new.h 已回收站删除 | 已完成 ✅ |
| **消隙过冲俯冲风险** | ✅ 已修复（2026-08-21） | pc/trajectory.py + state_machine.py | **已修复**：move_to/_move 增加 overshoot 参数；_servo_align、DESCEND（视觉+旧路线）、FINAL_ALIGN 全部禁过冲（过冲点在目标下方 ≈ 数十 mm，朝工作面逼近时会撞物）；SEARCH/HOME/APPROACH/LIFT/GRASP 保留消隙 | 阶段 9 实测确认 |
| **标定重投影误差单位标注错误** | ✅ 已修正（2026-08-21） | pc/calibration.py | err 实为像素（H_inv 把 mm 映回像素域），阈值 5.0 即 5px，注释已修正（legacy 备用路径） | 已完成 ✅ |
| 10000μF 电容效果 | 未验证 | 硬件 | 理论计算，未实测纹波 | 到货后示波器/观察 |
| PTC 8A 自恢复选型 | 推测值 | 硬件 | 3A 会误动作（6 舵机同时运动 >3A）；8A PTC 过热自动恢复，不用换 | 采购时确认 |
| 抓取成功率 ≥70% | 目标值 | 验收标准 | 未实测 | Phase E 验收 |
| YOLO 对 3.5cm 木块检测 | 已验证 | vision.py | best.pt 实测 conf=0.98（2026-08-16） | 已完成 ✅ |
| **舵机实际 270°（假设 180° 已推翻）** | 已验证事实 | 固件 main.h + usbd_cdc_interface.c | 用户确认舵机为 270° 型（2026-08-20）；脉宽映射已改 0-270°（500-2500µs，每度 7.4µs），编译 0 Error；限位表保守保持 0-180 待阶段 6 实测；**归中位置 = 上电默认 90°（1167µs），非舵机中点 135°** | 阶段 4 实测脉宽范围、阶段 6 实测限位 |
| **新模型 mAP50=0.995（2026-09-05）** | 训练指标，未实测 | pc/models/best.pt | 多尺度数据集（1155 张：orig/half/third/quarter/fifth）+ 原始 231 张混合训练，yolo26n-obb 基础模型，17 epoch early stop。**仅离线验证集指标，未在真实摄像头近景画面上测试** | 真机近景实测 |
| **多尺度裁剪坐标转换** | 未独立验证 | pc/build_multiscale_dataset.py | 从原图 OBB 标签偏移生成裁剪标签，理论正确但未画框抽查全部 1155 张（抽查 5 张通过） | 全量抽查或训练结果间接验证 |
| **Camera 1 分辨率 480×640** | 已实测 | vision_web.py | USB 摄像头实测 480×640，非高清；远距（木块占画面 <5%）可能检测不稳 | 联调时评估 |
| **SERVO_KP=0.15 未实测** | 推测值 | config.py | 基于"0.15×20px=3°>死区"的理论推算，**未在真机上调过** | Phase E 联调 |
| **CAMERA_OFFSET_X/Y=0** | 未标定 | config.py（已加入） | 相机-夹爪偏移未量测；已接入 `_servo_align` + `FINAL_ALIGN`（同一目标点），默认 0 = 旧行为 | 实测一次：夹爪对准木块时记录像素坐标 |
| **EMA_ALPHA=1.0（默认不平滑）** | 未调参 | config.py + state_machine.py（已实现） | 仅连续伺服 `_servo_align` 用（FINAL_ALIGN 停-看-动不用，跨移动平滑会混入旧位姿）；默认 1.0 = 直通旧行为；stub 冒烟验证 alpha=0.5 平滑生效 | Phase E 调参 |
| **下降判断用面积比** | 换算值未实测 | state_machine.py（已改） | 面积比（w×h/H×W）替代宽度比；阈值由旧宽度阈值数学换算（0.35²×4/3≈0.16、0.25²×4/3≈0.08，正方形 OBB + 4:3 画面假设），stub 冒烟通过，真机待复核 | Phase E 对比测试 |
| **JOINT_OFFSET 假设被校准实测推翻** | 已验证事实 | config.py + 固件 + kinematics.py | 老假设 J2 舵机 90°=大臂水平（offset 90）；S2 实测 **93°=水平**、S3 悬停位 167。`to_servo/from_servo` 映射与 IK 几何约定的零位参考均已失效（KNOWN_TRAPS #28），等真机标定统一重推 | 真机标定时（用户告知竖直方向与各关节基准） |
| **J4 竖直约束绝对式零位参考** | 待标定 | state_machine.py（已改微分形式） | 绝对式 90−J2−J3 零位未标定，已改微分形式 ΔJ4=−ΔJ2−ΔJ3（标定无关）；末端"绝对竖直"的 J2/J3/J4 组合由用户真机告知后回填 IK 与 H 表 | 真机标定 |
| **固件重构编译验证** | ✅ 已验证（2026-09-06 基线） | Core/Src/*.c、Core/Inc/*.h（2026-09-06 重构） | UV4 CLI 基线编译 **0 Error 0 Warning**（Code=11948，MDK-Lite 上限内）；命令见 SESSION_LOG 2026-09-06 续篇。此前"未编译"状态销账；硬件行为仍待真机流程 tools/test_firmware_gpio_servo | 装机联调 |
| **低 z 可达性（肘下分支休眠）** | 待标定 | kinematics.py + config.py | ik_solve 已加肘下分支（肘下解 fk 闭环验证通过），但被 `JOINT_MIN[2]=5°` 挡住（肘下需 j3<0）——**标定日前桌面级放置/抓取仍不可达**，TRANSPORT/DESCEND/GRASP 会报明确 IK 超限错误。标定日需实测 S3 负角度行程：有 → 放宽 JOINT_MIN[2] 或重推角度约定，肘下分支自动激活；无 → 该几何形态本机械不可实现，需另设计放置策略。**另见下一行 L4 语义核对——若工具竖直下挂，低 z 问题可能整体为伪问题** | 真机标定 |
| **IK 目标语义（腕点 vs 爪尖）未核实** | 待实测（装机日第 0 项） | kinematics.py + config.py | L4=170mm（J4→爪尖）**未入运动学模型**（fk 只算到 J4 点，config 注释明示"本项目 IK 只用到 J4"）。若工具竖直下挂 L4，爪尖=J4 点下方 ~170mm，桌面级低 z 抓取可能**无需肘下分支**（腕点悬 ~L4+块高即可）——上"低 z 可达性"结论可能是模型缺 L4 的伪问题；若爪尖≈J4 点则 L4 注释失实需更正。实测方法与判读见《拼装与测试指导》阶段 6 第 5 项 | 装机日第 0 项 |
| **PX_TO_MM=0.5（像素→毫米换算）** | 未标定预置 | config.py（对齐分层节） | wrist-first 路由的 err_mm 换算系数纯为预置（依赖相机焦距/安装高度），仅用于层级路由判断，不影响方向/收敛判定；装机日标定板实测回填 | 阶段 8 标定 |
| **调试台 SVG 图几何模型** | 部分确认 + 待标定 | pc/ui/（config.py LINK_ZERO_DEG/DIAL_SIGN） | 中位姿态已由用户标注台确认（2026-09-09：S2 大臂中位竖直向上、S3 小臂/S4 手部水平向右、S2 枢轴在底部基座）；**待装机确认**：①各关节正方向（舵机+1° 连杆顺/逆时针，DIAL_SIGN）②各关节角度范围（拖拽限位）③夹爪"<"朝向 ④S1/S5 俯视 0° 基准。校准工具已留：pc/ui/link_annotator.html（三连杆 360° 手动标注台） | 装机日（标注台 10 分钟校完） |
| **手套方案依赖未标定绝对运动学** | 已知债务依赖 | pc/glove/（方案 A/B） | 方案 A 基准位由 IK 可达点导出（GLOVE_SCH_A_REF_*），与失效的 JOINT_HOME/#28 解耦；仿真全部基于占位 JOINT_OFFSET/限位——**装机标定后映射结果会整体漂移，需重跑仿真重评** | 装机日标定后重跑 |
| **方案 B 工作域窄带 ~36mm** | 推测值（依赖占位限位） | pc/glove/mapping.py | JOINT_MIN/MAX 推测值 + elbow-up + 竖直腕 → (r,z) 可行域仅 ~36mm×36mm；舵机 270° 行程实测后限位表可能放宽、工作域随之扩大（重跑 `_validate_workspace`） | 装机日实测限位后 |
| **GLOVE 全部调参值为仿真值** | 未实测 | config.py GLOVE_ 节 | EMA α=0.2 / 限步 1.0° / 死区 0.5° / 中性区 5° / 增益 A×3 B×3 / 限位带 6°（MoveIt 默认）——全部仿真调参，**手感未验证** | 装机联调实测调参 |
| **手套链路（WiFi/ESP32 固件/真人手感）未实现未验证** | 未实现 | pc/glove/ + 待购硬件 | 仿真（合成 IMU 流）已验证滤波+映射算法；真实传感器、WiFi 链路、ESP32 固件、真人手感均未验证 | 硬件到货：有线联调 → WiFi → 手感标定 |
| **ESP32 手套固件真机未验证** | 未验证（仅主机侧） | glove-esp32/ | `pio run` 编译 0E0W（RAM 13.9%/Flash 57.7%）；C++ `mahony.h` vs `pc/glove/imu_filter.py` 主机数值 parity max diff **2.40e-05°**（clang++ 编译真实头文件）。**未验证**：MPU6050 实读、WiFi UDP 广播实际收包、串口 fallback、真实手姿态；**开机零偏校准与 CAL/ZERO 软件重零（零偏效果/静止度 metrics）**；MPU6050 轴向/零位装机标定；MPU6050 芯片真伪 | 硬件到货联调 |
| **限位域曾混乱（P0，已修 2026-09-10）** | ✅ 已修复 | config.py + kinematics.py | `JOINT_MIN/MAX/JOINT_HOME` 曾把**舵机域**数值当**关节域**用（J2/J4 差 90°）；现由 `SERVO_MIN/MAX`/`HOME_SERVO` 派生。**连带**：手套仿真方案 B 抖动判据 FAIL（需重跑重调）；IK 通过率 8→192。`JOINT_OFFSET` 仍待实测（#28） | 装机标定 |

---
### 2026-08-21 补充（外部审核验证后修复）
| 项 | 状态 |
|---|---|
| USART3 缺 UE 位 | ✅ 已修复（main.c CR1 加 USART_CR1_UE）|
| H 命令语义分裂 | ✅ 已修复（固件改发 JOINT_HOME 舵机角 [90,135,60,75,90,90]）|
| MALL/H 绕过软启动 | ✅ 部分修复（H 改逐关节 SetAngle；MALL 仍在但 PC 不用）|
| DRY_RUN clamp 不一致 | ✅ 已修复（改用固件 joint_min/max 表）|
| SEARCH 过冲 | ✅ 已修复（overshoot=False）|
| 超时被坏校验续命 | ✅ 已修复（last_cmd_tick 移到校验通过后）|
| USB CDC | ✅ 彻底删除（代码减 58%，7.20KB）|
