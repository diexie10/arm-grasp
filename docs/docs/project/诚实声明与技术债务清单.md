# 诚实声明与技术债务清单

> 记录所有"推测值""暂未验证的假设""无法在 CI 中测试的路径"。核心纪律：**不要假装**。推测值不可怕，假装是验证过的才可怕。

| 条目 | 类型 | 位置 | 说明 | 计划处理时间 |
|------|------|------|------|------------|
| IK 连杆参数 L1=72/L2=105/L3=128mm | 官方参数（待实测复核） | config.py | 来源：官方示例 6DOF_RobotArm.ino（YFROBOT 热销6自由度异形机械手臂，用户确认购买型号，2026-08-18）；已回填 config.py，IK 闭环验证通过（FK(IK) 误差 <1mm）；**拼装后仍需直尺实测复核**（L4=170 为 J4 到夹爪尖，本项目 IK 只用到 J4 不配置） | 到货后实测复核 |
| 红外极性（遮挡=0） | 未验证假设 | 固件 main.c | QT30CM 模块极性未实测；若相反需固件取反一行 | 到货后 `I` 命令实测 |
| QT30CM 输出类型（推挽/开漏） | 未验证假设 | 硬件接线 | 推挽则内部上拉够用；开漏需外部 4.7kΩ 上拉 | 到货后万用表实测 |
| 关节限位值 J1[0,180] 等 | 推测值 | 固件+config.py | 舵机角域限位（固件 [0,30,10,0,45,30]/[180,180,150,180,135,120]），未实测机械限位 | 到货后实测调整 |
| JOINT_OFFSET 安装假设 | 推测值 | config.py | **关键**：J2 offset 90（舵机90°=水平）、J3 offset 0（舵机角=关节角）、J4/J5 offset 90。机械安装不同则 IK 全错 | 到货后逐关节实测"舵机 90° 时机械位置" |
| 过冲量 15° | 推测值 | 上位机 trajectory.py（待建） | 手册建议值（>最大虚位 8°），未实测 | Phase E 联调验证 |
| S 曲线参数 30°/s, 15°/s² | 推测值 | 上位机 config.py（待建） | 手册建议值（MG996 极限 60°/s 的 50%），未实测 | Phase E 调参 |
| 舵机死区 8~15μs | 推测值 | 知识库 | 手册值，未实测 | 抖动排查时验证 |
| USB 热插拔行为 | 不可验证 | 固件 | 无法在编译期验证，需真机 | Phase E 联调 |
| **USB 断开检测不可靠** | 设计事实 | 固件 usbd_conf.c | **STM32F103 USB IP 无物理断开中断**，`HAL_PCD_DisconnectCallback` 永不触发（已加注释）；拔线实际走 3ms 无 SOF→SUSP 挂起，挂起回调不停 PWM。**PWM 停止唯一兜底 = PC 端 10s 命令超时**（`CDC_TimeoutCheck`）。拔线后臂保持最后姿态最多 10s 才 limp | 已记录，Phase E 实测确认 |
| 堆余量仅 ~480B | 设计事实 | 固件 usbd_conf.c | `USBD_CDC_Init` malloc ~544B，堆 1KB（startup 0x400），余量 ~480B。够用但紧；若未来加 USB 功能需扩堆 | 已记录 |
| ~~**boot 时 6 舵机同时上电**~~ | ✅ 已修复（2026-08-21） | 固件 usbd_cdc_interface.c | **已修复**：boot 不再启动任何 PWM；每通道由首条 M 命令经 Servo_SetAngle 懒启动（servo_enabled 标志 + estop 门控），PC 软启动（M1..M6 逐个 200ms）真正生效；timeout 恢复只清标志不批量使能；H 命令保留全恢复（保持电流场景，注释说明）。同时修复 boot 脉宽矛盾（Pulse=1500 硬编码 → SERVO_HOME_PULSE=1166 宏推导，消除上电先跳 135° 再回 90° 的猛转） | 已完成 ✅ |
| **USB CDC 双轨残留** | ✅ 已清理（2026-08-21） | 固件 usbd_cdc_interface.c | **已清理**：CDC_Receive_FS 不再喂 rx_ring（USART3 唯一命令入口）；CDC_Init_FS 删隐蔽使能路径；CDC_DeInit_FS 改纯 stub（USB 总线事件不再停舵机/清 ring——主机重启不能让抓着的物体掉落）；删除死变量 usb_disconnected/tx_busy 及 M 命令路径的 usb_disconnected 检查（USB 弃用后该检查会卡 1 拒绝所有 USART3 命令）；孤儿头 stm32f103xb_new.h/stm32f1xx_new.h 已回收站删除 | 已完成 ✅ |
| **消隙过冲俯冲风险** | ✅ 已修复（2026-08-21） | pc/trajectory.py + state_machine.py | **已修复**：move_to/_move 增加 overshoot 参数；_servo_align、DESCEND（视觉+旧路线）、FINAL_ALIGN 全部禁过冲（过冲点在目标下方 ≈ 数十 mm，朝工作面逼近时会撞物）；SEARCH/HOME/APPROACH/LIFT/GRASP 保留消隙 | 阶段 9 实测确认 |
| **标定重投影误差单位标注错误** | ✅ 已修正（2026-08-21） | pc/calibration.py | err 实为像素（H_inv 把 mm 映回像素域），阈值 5.0 即 5px，注释已修正（legacy 备用路径） | 已完成 ✅ |
| 10000μF 电容效果 | 未验证 | 硬件 | 理论计算，未实测纹波 | 到货后示波器/观察 |
| PTC 8A 自恢复选型 | 推测值 | 硬件 | 3A 会误动作（6 舵机同时运动 >3A）；8A PTC 过热自动恢复，不用换 | 采购时确认 |
| 抓取成功率 ≥70% | 目标值 | 验收标准 | 未实测 | Phase E 验收 |
| YOLO 对 3.5cm 木块检测 | 已验证 | vision.py | best.pt 实测 conf=0.98（2026-08-16） | 已完成 ✅ |
| **舵机实际 270°（假设 180° 已推翻）** | 已验证事实 | 固件 main.h + usbd_cdc_interface.c | 用户确认舵机为 270° 型（2026-08-20）；脉宽映射已改 0-270°（500-2500µs，每度 7.4µs），编译 0 Error；限位表保守保持 0-180 待阶段 6 实测；**归中位置 = 上电默认 90°（1167µs），非舵机中点 135°** | 阶段 4 实测脉宽范围、阶段 6 实测限位 |

---
### 2026-08-21 补充（外部审核验证后修复）
| 项 | 状态 |
|---|---|
| USART3 缺 UE 位 | ✅ 已修复（main.c CR1 加 USART_CR1_UE）|
| H 命令语义分裂 | ✅ 已修复（固件改发 JOINT_HOME 舵机角 [90,135,60,75,90,90]）|
| MALL/H 绕过软启动 | ✅ 部分修复（H 改逐关节 SetAngle；MALL 仍在但 PC 不用）|
| DRY_RUN clamp 不一致 | ✅ 已修复（改用固件 joint_min/max 表）|
| SEARCH 过冲 | ✅ 已修复（overshoot=False）|
| 超时被坏校验续命 | ✅ 已修复（last_cmd_tick 移到校验通过后）|
| USB CDC | ✅ 彻底删除（代码减 58%，7.20KB）|
